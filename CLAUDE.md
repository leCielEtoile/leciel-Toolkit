
## Claude Code 指示

### 全体ルール
- 回答が不確かな場合「可能性」などの言葉は使わず決めつけはせずに公式ドキュメント等を参照する
- コンテキスト削減のため必要に応じて積極的にエージェントを活用する
- プロンプトに対する不明点がある場合や課題に直面し別案を実行する場合はユーザーに承認を求める

### 作業前
- 作業対象に応じて AI コンテキストを読む `docs/AI-CONTEXT.md`
- タスク前に関連ファイルを読んでパターンを把握してからコーディング
- ユーザーから質問を受けた場合回答で返し実装はしない
- 実装を行う際は必ず計画を立てユーザーに承認を求める

### 作業中
- 既存ファイルの編集を優先。不要な抽象化・リファクタリングは行わない
- TypeScript の型定義を確認してから実装
- `.env` をコミットしない
- プロンプトに対し不明点や実行不能な課題に直面し別案を実行する場合は**必ずユーザーに質問を求める**こと
- コマンド実行の際heredocは使用してはならない 数行に連なる際は/tmpにシェルを作成し実行する

### デプロイ
- このリポジトリは Cloudflare と連携しており、`main` へのプッシュで自動デプロイされる
- 本番 URL: `https://tool.leciel.site`
- **モック・開発中など機能が未完成の状態では `git push` を行わない。コミットのみにとどめること**
- 機能が完成しローカルテストで動作確認できた段階で `git push origin main` を実行すること

### AI コンテキストの更新
コードを変更した際、内容が変わった場合は対応する ドキュメントも同時に更新すること:
- `docs/AI-CONTEXT.md`
- `README.md`

### 出力
**最終的なユーザーへの出力は必ず日本語で回答すること**